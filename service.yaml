apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: log4javasample
spec:
  template:
    spec:
      containers:
        - image: gcr.io/prepro-273413/log4java
                  ports:
            - name: http1
              containerPort: 80